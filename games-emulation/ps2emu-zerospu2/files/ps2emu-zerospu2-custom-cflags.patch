--- configure.ac.bak	2008-08-10 12:57:33.000000000 -0400
+++ configure.ac	2008-08-10 13:04:53.000000000 -0400
@@ -24,10 +24,6 @@
 AC_SUBST(ZEROSPU2_RELEASE)
 AC_SUBST(ZEROSPU2_SONAME)
 
-CFLAGS=
-CPPFLAGS=
-CXXFLAGS=
-
 dnl Check for debug build
 AC_MSG_CHECKING(debug build)
 AC_ARG_ENABLE(debug, AC_HELP_STRING([--enable-debug], [debug build]),
@@ -35,14 +31,11 @@
 if test "x$debug" == xyes
 then
  AC_DEFINE(_DEBUG,1,[_DEBUG])
- CFLAGS+="-g "
- CPPFLAGS+="-g "
- CXXFLAGS+="-g "
 else
  AC_DEFINE(NDEBUG,1,[NDEBUG])
- CFLAGS+="-O3 -fomit-frame-pointer "
- CPPFLAGS+="-O3 -fomit-frame-pointer "
- CXXFLAGS+="-O3 -fomit-frame-pointer "
+ CFLAGS+=" -O3 -fomit-frame-pointer "
+ CPPFLAGS+=" -O3 -fomit-frame-pointer "
+ CXXFLAGS+=" -O3 -fomit-frame-pointer "
 fi
 AM_CONDITIONAL(DEBUGBUILD, test x$debug = xyes)
 AC_MSG_RESULT($debug)
